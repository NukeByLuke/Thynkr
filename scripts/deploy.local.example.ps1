# Copy this file to deploy.local.ps1 and customize values.
# deploy.local.ps1 is gitignored and safe for private machine-only settings.

$SERVER = "root@your-server-ip"
$DOCKER_USER = "your-dockerhub-user"
